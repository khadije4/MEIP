import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useCopy } from '../DataDisplay'
export function GdpBeforeAfterChart({baselineGdp,sectorValue,directLoss}:{baselineGdp:number;sectorValue:number;directLoss:number}){const c=useCopy();const unaffected=baselineGdp-sectorValue,remaining=sectorValue-directLoss;const data=[{name:c('Avant le choc','قبل الصدمة'),unaffected,sector:sectorValue,loss:0},{name:c('Après le choc','بعد الصدمة'),unaffected,sector:remaining,loss:directLoss}];return <div data-testid="gdp-before-after-chart" className="h-80 w-full" role="img" aria-label={c('Composition du PIB avant et après le choc','تركيب الناتج قبل الصدمة وبعدها')}><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis width={76}/><Tooltip formatter={value=>{const n=Number(value);return [`${n.toLocaleString()} (${(n/baselineGdp*100).toFixed(2)}%)`,c('Millions MRU','مليون أوقية')]}}/><Legend/><Bar dataKey="unaffected" name={c('Économie non affectée','الاقتصاد غير المتأثر')} stackId="gdp" fill="#1d3557"/><Bar dataKey="sector" name={c('Secteur sélectionné restant','القطاع المحدد المتبقي')} stackId="gdp" fill="#16865c"/><Bar dataKey="loss" name={c('Perte directe','الخسارة المباشرة')} stackId="gdp" fill="#e85d3f"/></BarChart></ResponsiveContainer></div>}
