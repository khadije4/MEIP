import { Bar, BarChart, CartesianGrid, Cell, LabelList, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useCopy } from '../DataDisplay'
export function GdpShockWaterfallChart({baselineGdp,directLoss,simulatedGdp}:{baselineGdp:number;directLoss:number;simulatedGdp:number}){const c=useCopy();const data=[{name:c('PIB initial','الناتج الأولي'),base:0,value:baselineGdp,type:'baseline'},{name:c('Perte directe','الخسارة المباشرة'),base:simulatedGdp,value:directLoss,type:'loss'},{name:c('PIB simulé','الناتج المحاكى'),base:0,value:simulatedGdp,type:'result'}];return <div data-testid="waterfall-chart" className="h-80 w-full" role="img" aria-label={c('Cascade de l’impact sur le PIB','مخطط شلال أثر الناتج')}><ResponsiveContainer><BarChart data={data} margin={{top:28,right:10,left:12,bottom:8}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis width={76}/><Tooltip formatter={(value,name)=>[`${Number(value).toLocaleString()} ${c('millions MRU','مليون أوقية')}`,name==='value'?c('Valeur','القيمة'):'']}/><Bar dataKey="base" stackId="waterfall" fill="transparent"/><Bar dataKey="value" stackId="waterfall"><LabelList dataKey="value" position="top" formatter={(v:unknown)=>Number(v).toLocaleString()}/>{data.map(row=><Cell key={row.name} fill={row.type==='loss'?'#e85d3f':row.type==='result'?'#16865c':'#1d3557'}/>)}</Bar></BarChart></ResponsiveContainer></div>}
