import { useMemo, useState } from 'react'
import type { Indicator } from '../../types/economic'
import { SelectField, useCopy } from '../DataDisplay'
export type ScenarioPreset={code:string;fr:string;ar:string;sector:string;shock:number}
export function ScenarioForm({years,sectors,year,sectorCode,shockPercent,presets,onYear,onSector,onShock,onPreset,onAdvanced,onReset,advancedBusy}:{years:number[];sectors:Indicator[];year:number;sectorCode:string;shockPercent:number;presets:ScenarioPreset[];onYear:(v:number)=>void;onSector:(v:string)=>void;onShock:(v:number)=>void;onPreset:(v:string)=>void;onAdvanced:()=>void;onReset:()=>void;advancedBusy:boolean}){const c=useCopy();const [query,setQuery]=useState('');const clamp=(v:number)=>onShock(Math.min(100,Math.max(0,v)));const filtered=useMemo(()=>sectors.filter(i=>`${i.name_fr} ${i.name_ar??''} ${i.code}`.toLowerCase().includes(query.toLowerCase())),[query,sectors]);const selected=sectors.find(i=>i.code===sectorCode)
 return <div className="grid gap-5"><p className="rounded-2xl border border-gold-200 bg-gold-50 p-4 text-lg font-bold leading-8">{c(`En ${year}, que se passerait-il si ${selected?.name_fr??sectorCode} diminuait de ${shockPercent}% ?`,`في سنة ${year}، ماذا سيحدث إذا انخفض ${selected?.name_ar??sectorCode} بنسبة ${shockPercent}٪؟`)}</p><SelectField label={c('Scénario prédéfini','سيناريو جاهز')} value="" onChange={onPreset}><option value="">{c('Personnalisé','مخصص')}</option>{presets.map(p=><option value={p.code} key={p.code}>{c(p.fr,p.ar)}</option>)}</SelectField><SelectField label={c('Année','السنة')} value={year} onChange={v=>onYear(+v)} testId="simulation-year">{[...years].reverse().map(y=><option key={y}>{y}</option>)}</SelectField><label className="grid gap-1 text-sm font-semibold"><span>{c('Rechercher un secteur','البحث عن قطاع')}</span><input value={query} onChange={e=>setQuery(e.target.value)} className="rounded-xl border px-3 py-2.5 focus:ring-2"/></label><SelectField label={c('Secteur affecté','القطاع المتأثر')} value={sectorCode} onChange={onSector} testId="simulation-sector">{filtered.map(i=><option value={i.code} key={i.code}>{c(i.name_fr,i.name_ar??i.name_fr)}</option>)}</SelectField><label className="grid gap-3 text-sm font-semibold"><span>{c('Intensité du choc','شدة الصدمة')}: {shockPercent}%</span><input data-testid="shock-slider" aria-label={c('Intensité du choc','شدة الصدمة')} type="range" min="0" max="100" value={shockPercent} onChange={e=>clamp(+e.target.value)} className="accent-orange-600"/></label><label className="grid gap-1 text-sm font-semibold"><span>{c('Pourcentage exact','النسبة الدقيقة')}</span><input aria-label={c('Pourcentage exact du choc','النسبة الدقيقة للصدمة')} type="number" min="0" max="100" value={shockPercent} onChange={e=>clamp(+e.target.value)} className="rounded-xl border px-3 py-2.5 tabular-nums"/></label><div className="flex flex-wrap gap-2">{[10,25,50,75,100].map(v=><button type="button" key={v} onClick={()=>onShock(v)} className={`rounded-full px-3 py-2 text-sm font-bold focus:ring-2 ${shockPercent===v?'bg-gold-400':'bg-slate-100'}`}>{v}%</button>)}</div><div className="flex flex-wrap gap-3"><button type="button" onClick={onReset} className="rounded-xl border px-4 py-3 font-bold">{c('Réinitialiser','إعادة تعيين')}</button><button type="button" disabled={advancedBusy} onClick={onAdvanced} className="rounded-xl bg-mauritania-700 px-4 py-3 font-bold text-white disabled:opacity-50">{advancedBusy?c('Chargement…','تحميل…'):c('Générer le plan détaillé','إنشاء الخطة المفصلة')}</button></div></div>}
