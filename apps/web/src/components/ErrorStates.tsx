import type { ReactNode } from 'react'
import { useCopy } from './DataDisplay'
export function RetryButton({onRetry}:{onRetry:()=>void}){const c=useCopy();return <button type="button" onClick={onRetry} className="mt-4 min-h-11 rounded-xl bg-mauritania-700 px-4 py-2 font-bold text-white focus:ring-2">{c('Réessayer','إعادة المحاولة')}</button>}
export function SectionErrorState({onRetry,details}:{onRetry?:()=>void;details?:string}){const c=useCopy();return <section role="alert" className="rounded-2xl border border-red-200 bg-red-50 p-5"><h2 className="font-bold">{c('Impossible de charger ces données','تعذر تحميل هذه البيانات')}</h2><p className="mt-2 text-sm">{c('Vérifiez la connexion au serveur puis réessayez.','تحقق من الاتصال بالخادم ثم أعد المحاولة.')}</p>{onRetry&&<RetryButton onRetry={onRetry}/>} {details&&<details className="mt-3 text-xs"><summary>{c('Détails techniques','تفاصيل تقنية')}</summary><p className="mt-2">{details}</p></details>}</section>}
export function ChartErrorState(props:{onRetry?:()=>void;details?:string}){return <div data-testid="chart-error"><SectionErrorState {...props}/></div>}
export function BackendUnavailableBanner({children}:{children?:ReactNode}){const c=useCopy();return <aside role="alert" className="border-b border-red-200 bg-red-50 px-4 py-3 text-center text-sm font-semibold text-red-900">{children??c('Le serveur de données est indisponible. Certaines sections peuvent être incomplètes.','خادم البيانات غير متاح. قد تكون بعض الأقسام غير مكتملة.')}</aside>}
