# apps/web

Phase 4 frontend foundation: Vite, React, TypeScript, Tailwind CSS, routing,
i18next, Arabic RTL support, Axios, shared layout, error boundary, and Home.

```powershell
npm install
npm run dev
npm test
npm run build
```
